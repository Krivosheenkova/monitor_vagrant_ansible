global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "node"
    static_configs:
      - targets: ["{{ ansible_default_ipv4.address }}:9100"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["{{ prometheus_scrap_target }}"]